- uninstaller (prerm; but issues with removing directories since files.txt doesn't include those)
- GUI for installer (or automatic, at install time? discuss)
- (probably) requires release of new mobilesubstrate to load into assistantd
- finish control file: dependencies, description, support (who?), etc
- testing!
